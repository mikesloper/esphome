---
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: true
  min_auth_mode: WPA2
  use_psram: true  
  on_connect:
    # Let LVGL settle before BLE stack init (avoids display flash under load)
    - delay: 8s
    - ble.enable:
  ap:
    password: "password"

# captive_portal:

# web_server:
#   port: 80
#   version: 3
