---
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: true
  min_auth_mode: WPA2
  use_psram: true
  on_connect:
    then:
      - delay: 8s
      - ble.enable:
  ap:
    password: "password"
